# Frp on Docker Compose
A full-fledged example of an frp configuration.

## Usage

Start frps on server A:
```shell
$ docker compose up -d frps
```

Start frpc on server B:
```shell
$ docker compose up -d frpc
```